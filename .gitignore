.DS_Store
build/
.gradle/
!gradle/wrapper/gradle-wrapper.properties

# IntelliJ IDEA (IDE 개인 설정 — 레포에 커밋하지 않음)
.idea/

# 환경변수 · 시크릿 (이 저장소는 PUBLIC 이다 — 실제 값은 절대 커밋하지 않는다)
.env
.env.*
!.env.example

# 에이전트 개인 메모 (머신마다 다른 로컬 환경 — 커밋하지 않음)
CLAUDE.local.md
