# 운영 — 모든 시크릿은 환경변수 주입 필수 (기본값 없음)
# dev 와 같은 Supabase 프로젝트를 쓰고 스키마로만 갈라진다.
db:
  schema: ${DB_SCHEMA:petkok_prod}

spring:
  jpa:
    properties:
      hibernate:
        format_sql: false

logging:
  level:
    root: info
    org.hibernate.SQL: warn
